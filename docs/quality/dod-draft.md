# Черновик DoD

## 1. DoD для артефакта: Golden Dataset (План + данные)


| # | Критерий (Что проверяем)               | Тип проверки | Способ проверки (Скрипт / Действие)                                                                           | Целевое значение (Порог) | Статус |
| :--- |:---------------------------------------| :--- |:--------------------------------------------------------------------------------------------------------------| :--- |:-------|
| 1.1 | Файл датасета существует в репозитории | Есть/Нет | `test -f data/golden/golden_v1.jsonl`                                                                         | `true` (Exit code 0) |        |
| 1.2 | Минимальный объём ядра датасета        | Число | `wc -l data/golden/golden_v1.jsonl`                                                                           | `>= 10` строк |        |
| 1.3 | Валидность формата JSONL               | Есть/Нет |` Скрипт ` (проверяет парсинг каждой строки)                                                                    | `Exit code 0` (без ошибок парсинга) |        |
| 1.4 | Наличие обязательных ключей в JSON     | Есть/Нет | Скрипт проверяет наличие ключей: `id`, `image_path`, `ground_truth_score`, `adversarial_flag` во всех строках | `100%` строк содержат все ключи |        |
| 1.5 | Физическое наличие изображений         | Есть/Нет | Скрипт проходит по всем `image_path` и проверяет `os.path.exists()`                                           | `100%` путей ведут к существующим файлам |        |
| 1.6 | Покрытие болей (обманные атаки)        | Число | `grep -c '"adversarial_flag": true' data/golden/golden_v1.0.jsonl`                                            | `>= 1` пример |        |
| 1.7 | Покрытие болей (краевые случаи)        | Число | `grep -c '"edge_case_flag": true' data/golden/golden_v1.0.jsonl`                                              | `>= 1` пример |        |


## 2. DoD для артефакта: Критерии успеха и метрики

| # | Критерий (Что проверяем) | Тип проверки | Способ проверки (Скрипт / Действие) | Целевое значение (Порог)                  | Статус |
| :--- | :--- | :--- | :--- |:------------------------------------------|:-------|
| 2.1 | Документ существует и доступен | Ссылка | Наличие файла `docs/quality/success-criteria.md` и ссылка на него в `README.md` | Файл есть, ссылка рабочая                 |        |
| 2.2 | Для каждой метрики есть формула | Есть/Нет | Скрипт проверяет, что в таблицах метрик колонка "Способ подсчёта" не пустая (длина строки > 10 символов) | `100%` заполненных ячеек в колонке формул |        |
| 2.3 | Указаны целевые числовые значения | Число | Скрипт ищет паттерны чисел (например, `> 80%`, `< 5`, `100%`) в колонке "Целевое значение" | `>= 5` метрик с числовыми значениями      |        |


## 3. DoD для артефакта: Скрипты автопроверки

| # | Критерий (Что проверяем) | Тип проверки | Способ проверки (Скрипт / Действие) | Целевое значение (Порог) | Статус |
| :--- | :--- | :--- | :--- | :--- |:-------|
| 3.1 | Возможность запуска одной командой | Есть/Нет | Выполнение команды `make run_evals` (или `bash scripts/run_evals.sh`) в чистом окружении | `Exit code 0` (без фатальных ошибок) |        |
| 3.2 | Генерация отчёта о проверке | Есть/Нет | После запуска скрипта в папке `results/` появляется файл `eval_report.json` (или `.md`) | Файл существует, размер > 0 байт |        |
| 3.3 | Отчёт содержит ключевые метрики | Есть/Нет | Скрипт парсит `eval_report.json` и ищет ключи `score_delta` и `format_validation` | Ключи присутствуют в JSON |        |

